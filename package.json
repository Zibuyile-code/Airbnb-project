{
  "name": "airbnb-clone-admin",
  "version": "1.0.0",
  "description": "Airbnb Clone Admin Dashboard - Full Stack Application",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "start": "node backend/server.js",
    "start:local": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "heroku-postbuild": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
